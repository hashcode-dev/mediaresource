runtime: java
env: flex
env_variables:
  MONGO_DB_URL_DEV: "%MONGO_DB_URL%"
  COLLECTION_NAME_DEV: "%MONGO_DB_COLLECTION_NAME%"
runtime_config:
  operating_system: ubuntu22
  runtime_version: 17
handlers:
  - url: /.*
    script: this field is required, but ignored

manual_scaling:
  instances: 1