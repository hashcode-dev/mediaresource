runtime: java
env: flex
env_variables:
  MONGO_DB_URL_DEV: "%MONGO_DB_URL_DEV%" # will be set in cloud_env.sh
  COLLECTION_NAME_DEV: "%COLLECTION_NAME_DEV%" # will be set in cloud_env.sh
runtime_config:
  operating_system: ubuntu22
  runtime_version: 17
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1