runtime: java
env: flex
env_variables:
  MONGODB_DB_URL_DEV: "%MONGODB_URL_VALUE%" # will be set in cloud_env.sh
  COLLECTION_NAME_DEV: "%COLLECTION_NAME_VALUE%" # will be set in cloud_env.sh
runtime_config:
  operating_system: ubuntu22
  runtime_version: 17
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1